import React from 'react';
import { Button } from '@material-ui/core';

const App: React.FC = () => (
  <div className='app'>
    <Button variant='contained' color='primary'>
      Test button
    </Button>
  </div>
);

export default App;
